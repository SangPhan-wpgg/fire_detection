# Kết quả thí nghiệm

Mỗi lần chạy sử dụng một thư mục theo `experiment_id`, gồm cấu hình đóng băng,
metric, ghi chú và thông tin môi trường. Checkpoint lớn được lưu dưới
`artifacts/checkpoints/` (Ket qua chay duoc khong dua vao git).
