experiment_id: yolov8n_detection_v4_640_seed42
task: detect
model: yolov8n.pt
data: configs/data/detection.yaml

train:
  imgsz: 640
  epochs: 200
  batch: 64
  patience: 50
  save_period: 50
  optimizer: auto
  lr0: 0.01
  lrf: 0.01
  seed: 42
  deterministic: true
  device: 0
  project: artifacts/checkpoints/detection
  name: yolov8n_detection_v4_640_seed42

evaluation:
  split: test
  # Giữ confidence thấp khi tính AP; chọn ngưỡng vận hành trên validation riêng.
  conf: 0.001
  iou: 0.7
  save_json: true
  project: artifacts/metrics
  name: yolov8n_detection_v4_640_seed42
